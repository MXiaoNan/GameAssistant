<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <android.support.v4.view.ViewPager
        android:id="@+id/vp_show"
        android:layout_width="match_parent"
        android:layout_height="match_parent" />

    <Button
        android:id="@+id/btn_welcome"
        android:layout_width="100dp"
        android:layout_height="50dp"
        android:layout_alignParentBottom="true"
        android:layout_centerInParent="true"
        android:layout_marginBottom="60dp"
        android:background="@drawable/sel_btn_welcome"
        android:text="立即体验"
        android:textColor="@color/sel_btn_welcome"
        android:visibility="gone" />

    <ImageView
        android:id="@+id/iv_textlogo"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerInParent="true"
        android:src="@mipmap/bg_logo_font"
        android:visibility="gone" />

    <ImageView
        android:id="@+id/iv_piclogo"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_above="@id/iv_textlogo"
        android:layout_centerHorizontal="true"
        android:src="@mipmap/ic_about_app"
        android:visibility="gone" />
</RelativeLayout>